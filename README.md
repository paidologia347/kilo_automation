# kilo_automation

## Railway environment variables

Set the following environment variables in Railway before running uploads:

- `FTP_HOST=your-ftp-host`
- `FTP_USER=your-ftp-username`
- `FTP_PASS=your-ftp-password`

The FTP uploader uses Python's built-in `ftplib`, so no additional dependency is required.
